{
  "name": "mydict",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight desktop translator workspace",
  "scripts": {
    "bootstrap": "npm ci && npm --prefix frontend ci",
    "dev": "npm run dev:app",
    "dev:app": "npm run tauri -- dev --config src-tauri/tauri.conf.json",
    "dev:web": "npm run dev:frontend",
    "dev:frontend": "npm --prefix frontend run dev",
    "tauri": "npx tauri",
    "build": "npm run build:app",
    "build:app": "npm run tauri -- build --config src-tauri/tauri.conf.json",
    "build:frontend": "npm --prefix frontend run build",
    "typecheck": "npm --prefix frontend run typecheck",
    "lint": "npm --prefix frontend run lint && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features",
    "format": "npm --prefix frontend run format && cargo fmt --manifest-path src-tauri/Cargo.toml",
    "format:check": "npm --prefix frontend run format:check && cargo fmt --manifest-path src-tauri/Cargo.toml -- --check",
    "test": "npm run test:frontend && npm run test:rust",
    "test:frontend": "npm --prefix frontend run test",
    "test:rust": "./scripts/test-rust.sh",
    "check": "npm run typecheck && npm run lint && npm run test"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0"
  }
}
